.first
  h1
    | Keywords {{keywords.length}}
  p(ng-repeat='keyword in keywords')
    a(href="", ng-click="remove(keyword.keyword)")
      | {{keyword.keyword}} : {{keyword.count}}
    |  -------- 
    a(href='', ng-click="setWhitelist(keyword.keyword)")
      | OK
.second
  h1
    | Blacklist {{blacklist.length}}
  p(ng-repeat='keyword in blacklist')
    a(href="", ng-click="remove(keyword.keyword)")
      | {{keyword.keyword}}
  
.third
  h1
    | Whitelist {{whitelist.length}}
  p(ng-repeat='keyword in whitelist')
    a(href="" ng-click="")
      | {{keyword.keyword}} : {{keyword.count}}
